import {buildPositions} from './core.js';
const K='srvn:';const get=(k,d)=>{try{const v=localStorage.getItem(K+k);return v?JSON.parse(v):structuredClone(d)}catch{return structuredClone(d)}};const fmt=(n,d=0)=>n==null||!Number.isFinite(+n)?'—':new Intl.NumberFormat('vi-VN',{maximumFractionDigits:d}).format(+n);const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
function enhance(){const root=document.querySelector('#view-portfolio');if(!root)return;const q=root.querySelector('#tq'),btn=root.querySelector('#ta');if(q&&!root.querySelector('#tSide')){const side=document.createElement('select');side.id='tSide';side.className='input';side.innerHTML='<option value="BUY">MUA</option><option value="SELL">BÁN</option>';q.before(side);q.placeholder='Số lượng';const hint=document.createElement('div');hint.className='small';hint.id='tradeSideHint';hint.style.gridColumn='1/-1';hint.textContent='Giao dịch MUA/BÁN được lưu vào sổ giao dịch; lệnh BÁN không thể vượt quá số lượng cổ phiếu đang nắm giữ.';btn?.parentElement?.appendChild(hint)}const old=root.querySelector('#realizedPnlCard');old?.remove();const tx=get('tx',[]),market=get('market',{}),ps=buildPositions(tx,market),realized=ps.reduce((a,p)=>a+(p.realizedPnl||0),0),closed=ps.filter(p=>p.qty===0&&(p.realizedPnl||p.oversoldQty));const card=document.createElement('div');card.id='realizedPnlCard';card.className='section card';card.innerHTML=`<h2>Lãi/Lỗ đã thực hiện: <span class="${realized>=0?'good':'bad'}">${fmt(realized)}</span></h2><div class="small">Lãi/lỗ đã chốt được tách riêng khỏi lãi/lỗ của các vị thế đang nắm giữ. ${closed.length?`Đã đóng/không còn vị thế: ${closed.map(x=>`<b>${esc(x.symbol)}</b>`).join(', ')}`:''}</div>${ps.filter(p=>p.realizedPnl||p.oversoldQty).map(p=>`<div class="kpiRow"><span>${esc(p.symbol)}${p.oversoldQty?` • số lượng bán vượt vị thế đã bị chặn: ${fmt(p.oversoldQty)}`:''}</span><b class="${(p.realizedPnl||0)>=0?'good':'bad'}">${fmt(p.realizedPnl)}</b></div>`).join('')}`;root.appendChild(card)}
document.addEventListener('click',e=>{if(e.target?.id!=='ta')return;const side=document.querySelector('#tSide')?.value,q=document.querySelector('#tq');if(!q)return;const n=Math.abs(Number(q.value)||0);if(n)q.value=side==='SELL'?-n:n},true);
let timer;new MutationObserver(()=>{clearTimeout(timer);timer=setTimeout(enhance,150)}).observe(document.body,{childList:true,subtree:true});if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',enhance);else enhance();
